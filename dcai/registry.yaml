# DCAI Basic Mode Command Registry
# Generic developer-friendly commands.
# Commands with "usage" take arguments — the arg is prompted after selection.

commands:
  open:
    description: Open any application (e.g. open firefox, open code, open terminal)
    usage: "open <app-name>"
    action: run_in_terminal

  edit:
    description: Edit any file in the default editor (e.g. edit ~/.vimrc, edit src/main.py)
    usage: "edit <file-path>"
    action: run_in_terminal

  run:
    description: Run any shell command in a terminal (detects SSH vs desktop)
    usage: "run <command>"
    action: run_in_terminal

  sysinfo:
    description: Display CPU, RAM, GPU, and disk summary
    action: sysinfo_custom

  ps:
    description: List running processes sorted by memory usage
    action: "ps aux --sort=-%mem | less"

  kill:
    description: Kill a process by name
    usage: "kill <process-name>"
    action: "pkill -f"

  update:
    description: Run system package update for the detected package manager
    action: update_system_custom

  lock:
    description: Lock the screen
    action: lock_screen_custom

  screenshot:
    description: Take a screenshot interactively
    action: screenshot_custom

  vol-up:
    description: Increase volume by 10%
    action: "pactl set-sink-volume @DEFAULT_SINK@ +10% || amixer set Master 10%+"

  vol-down:
    description: Decrease volume by 10%
    action: "pactl set-sink-volume @DEFAULT_SINK@ -10% || amixer set Master 10%-"

  mute:
    description: Mute/unmute audio
    action: "pactl set-sink-mute @DEFAULT_SINK@ toggle || amixer set Master toggle"

  wifi:
    description: Toggle Wi-Fi on/off
    action: wifi_toggle_custom

  bluetooth:
    description: Toggle Bluetooth on/off
    action: bluetooth_toggle_custom
